let part1 = lines => {
  let n = Array.length(lines);
  let valid = ref(n);
  - valid^;
};

let part2 = lines => {
  let n = Array.length(lines);
  let valid = ref(n);
  - valid^;
};

let run = () => {
  let lines = Utils.getInput(1);
  Printf.printf("Part 1: %d\n", part1(lines));
  Printf.printf("Part 2: %d\n", part2(lines));
};
